{
  "name": "banana-grabber",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"cd frontend && yarn dev\" \"cd backend && yarn dev\"",
    "build": "yarn build --cwd frontend",
    "start": "yarn start --cwd frontend"
  },
  "dependencies": {
    "concurrently": "^8.0.1"
  }
}
